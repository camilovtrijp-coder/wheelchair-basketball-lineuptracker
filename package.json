{
  "name": "roba-stats-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Lineup Tracker PWA for basketball and sports teams",
  "main": "index.html",
  "workspaces": ["v2", "firebase"],
  "scripts": {
    "test": "playwright test",
    "test:unit": "npm --prefix v2 run test:unit",
    "test:all": "npm test && npm run test:unit"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
