{
  "name": "firebase-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PR 4.4 — begrensde Firebase-spike (Emulator Suite, uitsluitend fictieve data). Losstaand van v2/; zie docs/SPIKE_REPORT.md.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "seed": "tsx scripts/seed.ts",
    "type-check": "tsc --noEmit",
    "test:rules": "vitest run tests/rules",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium",
    "emulators": "firebase emulators:start --project demo-lineup-tracker-spike",
    "spike:verify": "npm run type-check && firebase emulators:exec --project demo-lineup-tracker-spike --only firestore,auth \"npm run seed && npm run test:rules && npm run seed && npm run test:e2e\""
  },
  "dependencies": {
    "firebase": "^12.17.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@playwright/test": "^1.62.1",
    "firebase-admin": "^14.2.0",
    "firebase-tools": "^15.25.1",
    "tsx": "^4.23.7",
    "typescript": "^5.5.4",
    "vite": "^5.4.6",
    "vitest": "^2.1.1"
  }
}
