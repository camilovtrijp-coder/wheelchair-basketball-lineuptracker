[build]
  # Alleen index.html wordt gepubliceerd. docs/ en README bevatten geen
  # gevoelige data meer, maar hoeven niet als losse bestanden mee de site in.
  command = "mkdir -p _site && cp index.html _site/index.html"
  publish = "_site"
