{
  "name": "firebase-base",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PR 5.1 — reproduceerbare Firebase-basis (Rules, indexes, seeddata, typed documentcontracten). Vervangt firebase-spike/ als fundament voor Fase 5; zie docs/IMPLEMENTATION_PLAN.md en firebase/docs/QUERY_CONTRACT.md.",
  "engines": {
    "node": ">=18"
  },
  "exports": {
    "./documents": {
      "types": "./src/documents/index.ts",
      "default": "./src/documents/index.ts"
    }
  },
  "scripts": {
    "seed": "tsx scripts/seed.ts",
    "type-check": "tsc --noEmit",
    "test:unit": "vitest run tests/unit",
    "test:rules": "vitest run tests/rules",
    "emulators": "firebase emulators:start --project demo-lineup-tracker-dev",
    "verify": "npm run type-check && npm run test:unit && firebase emulators:exec --project demo-lineup-tracker-dev --only firestore,auth \"npm run seed && npm run test:rules\""
  },
  "dependencies": {
    "firebase": "^12.17.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase-admin": "^14.2.0",
    "firebase-tools": "^15.25.1",
    "tsx": "^4.23.7",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
